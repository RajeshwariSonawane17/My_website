// Example: Optional JS for image hover animations or other interactivity
$(document).ready(function() {
    // Example: Submit form alert
    $('form').on('submit', function (e) {
        e.preventDefault();
        alert('Your email has been submitted!');
    });
});
